syntax = "proto2";

message SignatureKey {
    required bytes data = 1;
}

message CapabilityMessage {
    message Chain {
        required uint32 rights = 1;
        required SignatureKey entity = 2;
    }
    required bytes secret = 1;
    repeated Chain chain = 2;
}
